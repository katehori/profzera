/posts/admin/all:
  get:
    tags: [Admin]
    summary: Lista todos os posts (admin)
    security:
      - bearerAuth: []
    parameters:
      - $ref: '../../openapi.yaml#/components/parameters/page'
    responses:
      200:
        description: Lista completa (incluindo não publicados)
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../schemas/Post'
      403:
        $ref: '../schemas/error.yaml#/components/responses/Forbidden'